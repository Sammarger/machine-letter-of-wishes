# Queries

- In order for the N8N to work, the user must input exactly the right text for certain fields, for instance `Move File`, there should be a standard markdown that users are given, so we need to use dropdown option menus. 

- The markdown input fields must change depending on the inputs before it. For instance `Move File` will have a different set of necessary parameters to `Send Email`